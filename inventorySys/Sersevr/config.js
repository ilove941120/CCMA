const configs = {
    mysql:{
        host: '127.0.0.1', // 只指定主機地址，不包含端口號
        port: 3300, // 指定端口號
        user:'root',
        password:'1234',
        database:'ccma'
    }
}
module.exports = configs